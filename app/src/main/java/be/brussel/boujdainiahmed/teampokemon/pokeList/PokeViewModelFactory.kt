package be.brussel.boujdainiahmed.teampokemon.pokeList

/*
import android.app.Application
import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider
import be.brussel.boujdainiahmed.teampokemon.database.PokemonDAO
import be.brussel.boujdainiahmed.teampokemon.team.TeamViewModel

class PokeViewModelFactory (

    private val application: Application,
    private val dataSource: PokemonDAO): ViewModelProvider.Factory {
    @Suppress("unchecked_cast")
    override fun <T : ViewModel?> create(modelClass: Class<T>): T {
        if ( modelClass.isAssignableFrom(PokeViewModel::class.java)) {
            return  PokeViewModel(dataSource, application) as T
        }
        throw  IllegalArgumentException("Unknown ViewModel Class")
    }

}

 */